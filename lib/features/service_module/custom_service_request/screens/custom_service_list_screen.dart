import 'package:flutter/material.dart';

class CustomServiceListScreen extends StatelessWidget {
  const CustomServiceListScreen({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox();
}
