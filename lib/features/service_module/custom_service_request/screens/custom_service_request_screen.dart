import 'package:flutter/material.dart';

class CustomServiceRequestScreen extends StatelessWidget {
  const CustomServiceRequestScreen({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox();
}
