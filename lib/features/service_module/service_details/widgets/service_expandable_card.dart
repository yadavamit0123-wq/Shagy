import 'package:flutter/material.dart';

class ServiceExpandableCard extends StatelessWidget {
  const ServiceExpandableCard({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox();
}
